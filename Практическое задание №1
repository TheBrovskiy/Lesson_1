// Создать пустой проект в IntelliJ IDEA и прописать метод main()

public class Main {

    public static void main(String[] args) {
	// write your code here
    }
}

// Создать переменные всех пройденных типов данных и инициализировать их значения.

public class Main {

    public static void main(String[] args) {
	byte a = 2;
	short b = 4;
	int c = 6;
	long d = 8L;
	float e = 8.2f;
	double f = 10.4;
	char g = '%';
	boolean h = true;
	String i = "Строчное значение";
    }
}

// Написать метод, вычисляющий выражение a * (b + (c / d)) и возвращающий результат,
где a, b, c, d – аргументы этого метода, имеющие тип float.

public class Main {

	public class void main(String[] args) {
		System.out.println(equation(1.2f, 2.4f, 6.6f, 2.2f));
	}
	public static float equation(float a, float b, float c, float d) {
	return a * (b + (c / d));

    }
}

// Написать метод, принимающий на вход два целых числа и проверяющий, что их сумма лежит в пределах от 10 до 20 (включительно), если да – вернуть true, в противном 
случае – false.

public class Main {
    public static void main(String[] args) {
    go(2, 4);
        }
    static boolean go(int a, int b) {
        return a + b >= 10 && a + b <= 20;
    }
}

// Написать метод, которому в качестве параметра передается целое число, метод должен напечатать в консоль, положительное ли число передали или отрицательное. 
Замечание: ноль считаем положительным числом.

public class Main {
    public static void main(String[] args) {
    go(-2);
        }
    static void go(int a) {
    if (a >= 0) {
        System.out.println("Передано положительное число");
    } else {
        System.out.println("Передано отрицательное число");
    }
    }
}

// Написать метод, которому в качестве параметра передается целое число. Метод должен вернуть true, если число отрицательное, и вернуть false если положительное.

public class Main {
    public static void main(String[] args) {
    go(4);
        }
    static boolean go(int a) {
        return a < 0;
        }
    }

// Написать метод, которому в качестве параметра передается строка, обозначающая имя. Метод должен вывести в консоль сообщение «Привет, указанное_имя!».

public class Main {
    public static void main(String[] args) {
        print("Давид");
    }
        static void print(String name) {
            System.out.println("Привет, " + name + "!");
        }
}
